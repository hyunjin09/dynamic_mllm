# Access Policy Template

Copy this file to the Git-ignored `ACCESS_POLICY.md` and replace the
placeholders with roots approved for the current server.

Only access:

- `.`
- `<DATASET_ROOT>`
- `<MODEL_ROOT>`

Do not inspect or modify paths outside these roots without explicit user
authorization.
