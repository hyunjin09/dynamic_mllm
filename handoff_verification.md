# Handoff verification

Verified 2026-09-12 against `research_handoff.md`, current project/Phase-84 state, the six recommended scientific summaries, the latest Phase-84 plan, and its next-method recommendation. This is a summary-level reconciliation; raw outputs, code, checkpoints, and artifact hashes were not independently re-audited.

1. **Core hypothesis.** A frozen Qwen2.5-VL-7B can expose information in its hidden trajectory that identifies when visual computation is unnecessary or harmful and which READ/WRITE operations to suppress, preserving dense-correct answers while rescuing dense failures. Stage 1 predicts eventual dense failure; Stage 2 must predict corrective treatment value. Failure risk and correction utility are different targets.

2. **Established.** Dense failure is learnable in-domain, with strong source dependence. Exact counterfactual effects and correctness-changing interventions exist. With WRITE ON, `H_R = q_WRITE_ONLY - q_FULL` is positive on 7,285 and negative on 7,900 of 15,185 dense post-trigger states, with 625 harmful and 41 beneficial correctness flips. Here `q` is the frozen teacher-forced gold-answer score, distinct from generated-answer LMMS correctness. Phase 84 reports successful parity/validity checks and increasing effect magnitude (median H8/H1 norm ratio 2.268), without useful predictive emergence.

3. **Not supported.** No accepted Stage-2 deployment winner exists. The tested evidence supports neither benchmark-general Stage 1, a robust contiguous READ-harm regime, a reliable local attention/update mechanism, nor practical local/one-step/H≤8 treatment prediction. Local F_ALL/MLP Spearman is 0.0697; one-step delta is 0.0773. Phase-84 common-support delta Spearman is 0.0756/0.0688/0.0646/0.1097 at H1/H2/H4/H8. No horizon passes the prospective materiality gate: **H-READ-D**. These findings reject the tested families, not Stage-2 feasibility in principle.

4. **Unknown.** Whether longer, jointly chosen READ schedules add meaningful correction; whether successful plans can be identified without oracle answers; whether gains generalize across sources; whether continuous-score gains translate into LMMS rescue; and whether a resulting policy improves external accuracy or compute. READ-harm causes remain unresolved. The mechanism comparison contains only 26 matched harmful-versus-beneficial pairs, limiting negative mechanistic conclusions.

5. **Exact frontier.** Phase 84 is complete and stopped. It changed READ only at layer `l`, then used FULL continuation; H counts the intervention layer, so H8 includes seven subsequent FULL layers. Its primary comparison used 6,916 H8-eligible states / 872 UIDs, rather than all 15,185 states / 1,413 UIDs. Multi-layer READ schedules have not been tested in this current fixed-WRITE study. Earlier four-action/program searches do not resolve this particular question.

6. **Recorded next experiment and discriminator.** The existing recommendation is a separately authorized bounded longer-horizon READ planning/search audit using only FULL and WRITE_ONLY, with WRITE always ON and exact shared dense starting states. It would distinguish additional replay-valid plan existence from the weak single-toggle evidence; oracle success would still leave permitted-information ranking and controller learnability unresolved. A negative result would weigh against only the tested bounded extension. No standalone next-experiment plan was found: horizons, budget, candidates, saturation, objective, and validation rules remain to be prospectively specified. The latest available [plan](plans/read_short_horizon_counterfactual_propagation_plan.md) describes completed Phase 84; its [recommendation](analysis/read_harm_short_horizon_propagation/summaries/next_read_method_recommendation.md) explicitly supplies no execution authorization.

7. **Consistency findings.** No numerical or outcome-category contradiction was found between the handoff and the reviewed scientific summaries. H1 Spearman 0.0773 versus 0.0756 reflects native versus H8-common support, not failed replication. Older recommendations remain in the global state files but are superseded by Phase 84. The on-disk `AGENTS.md` omits the research-control routing and conditional-review provisions supplied in this session; the session instructions govern this takeover. The missing next-experiment specification agrees with the handoff's warning that the follow-on is not yet specified or authorized.

Only this verification file was created. No experiment was proposed or run, and no research plan or phase state was changed. Awaiting further instruction.
