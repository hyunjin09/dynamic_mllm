# Phase87 execution scope

The explicit user request selects `plans/write_harm_structure_learnability_plan.md`. This supersedes the prior stop-after-handoff and completes the named staged WRITE diagnostic. Four occupied GPUs remain explicitly authorized by the earlier user instruction; unrelated workloads are preserved. Phase85 search remains stopped.

No packages or datasets were installed. Core generation/label code and prior READ artifacts were not changed. Large features, normalizers, prediction task records, original-coordinate tensors and raw WRITE propagation states are retained under the allowed `/mnt/hyemin` root. The repository worktree was already heavily modified; no reset, cleanup, commit, merge or push was performed.

Full WRITE local parity, feature extraction, the structural census and 360 fixed training tasks completed before W3. The local gate is W-LOCAL-WEAK. W3 was separately frozen after independent protocol review, with explicit inconclusive treatment of mixed evidence, and starts with fresh replay smoke. It uses cached canonical FULL reached-layer tensors as ON references and freshly computes READ_ONLY intervention plus FULL continuation as OFF; target labels never change.

Tests: five focused local feature/metric tests, one mocked WRITE rollout semantics/boundary test, and one weighted-rank bootstrap equivalence test. The cached-order bootstrap keeps exact tied-rank and image-group resampling semantics while avoiding repeated sorting. Final evidence validation separately checks task completeness, contracts and artifact hashes.
