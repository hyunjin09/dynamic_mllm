# Next READ-method recommendation

Recommend exactly one next step: **run one short-horizon READ effect-propagation/planning audit instead of another local classifier**.

This is a recommendation, not authorization to execute it. Do not return directly to a four-action router.
