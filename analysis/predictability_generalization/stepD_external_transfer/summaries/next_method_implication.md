# One next-method implication

The next method must jointly address robust failure representation and nonlocal intervention selection; first distinguish source-calibration failure from representation failure before adding routing complexity.

Evidence: Step-D categories `D1-C` / `D2-A`, with the exact benchmark metrics and controls in the final summary.

This recommendation does not establish deployment gain, causal mechanism, or that richer Stage-2 information will succeed. It is not authorization to run the next experiment.
