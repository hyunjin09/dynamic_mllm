# Calibration determines category scope, not held-out efficacy

Complete CAL evidence: `calibration/selected_layers.csv`, `split_half_stability.csv`, and `bootstrap_layer_selection.csv`. Every primary bit is non-NONE. Only TextVQA WRITE and POPE READ satisfy both finite split-half rho >=0.3 and primary-selection bootstrap frequency >=0.5.

Thus no family has all active bits stable (A cannot pass), and neither named bit is stable in two families (B cannot pass). No family has both bits satisfying the frozen instability predicates (C cannot pass). Residual qualified CAL-D is therefore logically determined before held-out efficacy is analyzed, regardless of the later accuracy values. It must not be described as an outcome-based finding of no held-out benefit.

Research-control DEEP review: existing read-only reviewer verdict stable, confidence high. Ranking: complete frozen M1/M2/M3, global and20random comparisons > stop after CAL > revise the outcome-blind category contract. Strongest objection is the predetermined category and remaining cost. Reconciled decision: finish the explicitly authorized matrix because efficacy, bit interaction and global/random explanations remain unknown; report these separately from the category. No rubric revision, budget reduction, new experiment or strategic pivot.

All TEST outcomes remain excluded from schedule selection. Preserve any positive or negative intervals and POPE's 14 independent CAL image groups prominently. This review contains no held-out performance estimate.
