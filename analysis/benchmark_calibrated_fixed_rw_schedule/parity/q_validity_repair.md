# Validity repair before calibration

First dense pass stopped after209durable UIDs because external_answer_specs rejects empty normalized TextVQA references. All other workers were stopped by the supervisor. Metadata-only audit finds4TEST and1CAL TextVQA rows with invalid q; all remain in correctness evaluation. The existing protocol already says q is conditional on validity and skips q tie-breaking across the whole bit/pool if incomplete. Runner now stores q=null with explicit reason, and bootstrap selection follows the same no-q tie rule. No target, population, native scorer, selector, schedule or budget changed. This is implementation repair, not a negative research result. Old contract/smoke and raw outputs/logs are preserved; the tiny partial Dense stage is rerun under the amended code hash. No READ/WRITE intervention or held-out method has run.
