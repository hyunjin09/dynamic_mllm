# Prospective benchmark-calibrated fixed READ/WRITE schedules

User authority: `plans/benchmark_calibrated_fixed_read_write_schedule_plan.md`, requested 2026-09-14. One bounded phase. No Stage1 feature extraction, scores, trigger, P90 subset, training, MCTS/beam search, optional top-2, or automatic follow-on experiment.

The fixed model, native input builder, native scorers, generation budgets, BF16/SDPA and repetition penalty are inherited from the exact previous external evaluation. READ-off is WRITE_ONLY, WRITE-off is READ_ONLY, and same-layer combination is IGNORE. The fixed 28-layer schedule applies to every sample in a family. The route establishes heterogeneous prompt caches used throughout autoregressive decoding; no later adaptive policy runs.

Splits were frozen before intervention outcomes. CAL/TEST UID counts are ChartQA 256/2,500; TextVQA 255/5,000; MMMU-Pro 256/3,204; POPE 252/8,748. CAL image/content groups are 118/160/127/14 respectively. See `splits/leakage_check.md` for raw-byte/RGB/native-identity transitive grouping, train-pool availability limitations, and cross-variant protection. Whole-group prefixes can be smaller than target N, with no selective backfill. POPE's 252 questions represent only 14 independent images; uncertainty must preserve that dependence. TEST is not the full official population for MMMU-Pro or POPE. Report Standard/Vision and all three POPE variants separately, with family-pooled values for the four-family macro.

Binary correctness uses the inherited native scorer and threshold, including TextVQA EvalAI consensus ≥0.5, so Net=(W→C−C→W) exactly equals the sum of per-UID correctness changes. Mean native raw score, including fractional TextVQA consensus, is reported separately and is not silently renamed binary accuracy. q uses the previously defined evaluator-compatible finite gold strings: literal ChartQA answer, answer-frequency-weighted normalized TextVQA references, MMMU A–J, and POPE yes/no, aggregated from token-normalized mean log probabilities. q is a secondary surrogate, not a correctness replacement.

Execution order: freeze splits/protocol; native-dense parity smoke and complete dense baseline; forced-action/repeat/suffix-parity/terminal-WRITE smoke on CAL only; full READ 28-layer CAL sweep; full WRITE 28-layer CAL sweep; per-layer effects, nested-size and split-half/bootstrap analyses; freeze benchmark and global schedules and random registry; complete TEST M1/M2/M3; global; random; final paired analysis and category. No held-out intervention outcome enters selection. Existing exact same-UID native dense records may be reused only with fresh unified token/score equality for every consumed UID; fresh native inference is mandatory for new train CAL rows and seven-variant smoke. Store q for every dense row and every single-bit calibration branch. This verifies and stores Dense for all 20,471 selected CAL/TEST rows.

Both bit selectors maximize Gain=Net/N on the primary whole-group pool; return NONE if max Gain≤0. Resolve ties by higher MeanQ, fewer regressions, then lower layer. If any eligible layer lacks complete valid q on the same calibration pool, skip q for the entire bit selection and retain the remaining tie rules. WRITE layer27 is actually evaluated as a semantic zero-effect control and excluded from selection; READ uses layers0–27 and WRITE layers0–26. All results are retained, including negatives and NONE. No feature fitting or architecture selection.

Global calibration pools all four primary calibration sets with equal weight per UID, using the same selector. M1/M2 are single-bit ablations; M3 executes the real independent combination, without assuming additivity or choosing a replacement on TEST. One schedule is fixed per family, including both MMMU variants and all POPE variants. Different variants do not get separately tuned schedules.

Random control: prospectively reduce 100 to **20 schedules per family**, as permitted by section16, because 100×19,452 held-out UIDs is 1,945,200 route evaluations before required methods and calibration, versus 389,040 for20. For each index, independent frozen seeds draw each active bit's layer uniformly from its eligible range; absent bits stay absent. Coincident READ/WRITE layers become IGNORE. Sampling is with replacement; repeated routes are executed once and reused without changing their Monte Carlo weight. Calibrated percentile uses midranks, so ties to all identical NONE schedules give50%, not100%. No further outcome-driven control-budget reduction.

Statistical analysis: 5,000 paired image/content-group bootstrap draws; keep all co-image questions and paired method outcomes together. Four-family macro is the arithmetic mean of family deltas; bootstrap families' independent group samples and then average. Pooled-UID delta is secondary. Calibration selection stability uses2,000 group-bootstrap draws, selection frequencies including NONE, and entropy in nats. Split-half groups alternate their frozen order; report per-bit layer Spearman, selected top1 agreement, raw top3 overlap and sign agreement, with constant-vector correlation unestimable. Report actual whole-group N and full-pool rank of smaller-N selections. No smaller-N test-time schedule execution is required.

Frozen category precedence is CAL-A, then CAL-B, then CAL-C, otherwise qualified CAL-D. Replication requires at least two family-specific paired 95% CI lower bounds strictly above zero and a four-family macro lower bound above zero. For a stable contributing benchmark, every selected non-NONE bit must have finite split-half layer rho≥0.3 and at least0.5 bootstrap frequency for the primary selected layer. NONE and constant/undefined profiles are not classified as unstable. A requires M3 replication on at least two stable benchmarks, positive M3−GLOBAL macro CI, and M3 strictly exceeding all20random schedules on the same two benchmarks. Report Monte Carlo p=(1+#random accuracy≥M3 accuracy)/21 separately from the descriptive midrank percentile; with20draws the smallest attainable p is1/21. These are prospective diagnostic gates, not multiplicity-adjusted confirmatory family discoveries.

B requires the same named bit to replicate on at least two benchmarks where that bit meets the stability check, with a positive macro CI, while M3 fails held-out replication (fewer than two positive family CIs or nonpositive macro lower bound). If both bits qualify, report both but the eventual recommendation selects one using the frozen larger macro lower bound, then macro point gain, then READ tie rule. C requires both selected non-NONE bits to be unstable in at least two benchmarks: finite split-half rho<0.3, primary selected-layer bootstrap frequency<0.5, and more than one selected layer across nested N. It also requires M3 to fail replication. Constant vectors, unestimable correlations and NONE alone do not count as instability. All remaining patterns are D, meaning **no demonstrated control-supported fixed-schedule benefit under the full frozen rubric**; if M3 improves Dense but global/random or stability gates fail, explicitly call it mixed evidence, not absence of any held-out benefit. Preserve all per-benchmark positive and negative intervals, including any unclassified-looking pattern, alongside the one required primary category. POPE counts toward a multi-benchmark criterion only if its actual grouped held-out and stability gates pass.

Efficiency reports separate calibration GPU wall-hours, dense verification, and held-out evaluation costs. Prefix sharing and duplicate-route reuse are evaluation optimizations. Observed evaluation throughput is not a deployment latency or FLOP-saving claim. No end-to-end savings claim without calibration amortization; no deployment-efficiency benchmark is added.

Freeze binds split, protocol, model/processor files, executor, scorer, input builder, selectors, seeds and capacities (no learned model). Each durable UID-level stage record and exact action trace is hashed and reconciled against the census before advancing. Fresh generation mismatch, unexpected terminal WRITE effect, incomplete stage, or contract drift stops execution for repair. Complete the requested artifacts and exactly one unexecuted recommendation, then stop.


## Prospective Dense token-equivalence repair (before calibration)

A bounded dense-only replay of MMMU-Pro Standard test_Music_53 reproduces native `B.` and unified `B`, both score1/correct. Native exactly matches its stored parent in two fresh replays. Preserve native generated tokens/answer/score as authoritative Dense; q stays consistently defined by unified native-causal teacher forcing for all calibration branches. Any unified token mismatch requires two fresh native repeats and exact native-parent parity where a parent exists, plus exact unified/native scorer and binary-correctness equality. A score or correctness mismatch remains fatal. Save both outputs. Logical all-FULL schedules reuse native Dense. Terminal WRITE zero-effect compares token/q equality within the same unified executor and scorer equality to native Dense. Add the sentinel to dense-only smoke; TEST interventions remain forbidden before schedule freeze.

Retain already completed Dense rows only through the explicit hash-bound accepted-parent manifest and original contract. They already passed exact tokens and scorer parity, and no new intervention outcomes existed when this repair was selected. The same independent reviewer accepted this repair with high confidence; re-running unchanged validated rows adds no useful evidence. The correction implements the established native-authority lesson from Phase69; it changes no population, calibration selector, scorer, q definition or budget.

## Dense continuation validity amendment (before calibration)
The Psychology128 diagnostic identified exact prefill/logit agreement but a native-vs-cached continuation-position mismatch. Native Transformers5 generation increments the final prompt position, while the inherited decoder used full length plus a maximum-based RoPE delta. Phase88 now derives its effective decode delta as final prompt position+1−full prompt length, with equal final rotary planes and unpadded inputs asserted. The same metadata drives generation and q teacher forcing. Historical shared executor files remain unchanged. All partial Dense records from older contracts are archived and every Dense row/q is regenerated. Both identified MMMU sentinels must have exact native token parity; full population scorer parity remains mandatory. This is an execution-validity repair, with no CAL schedule selection or TEST intervention observed before amendment.
