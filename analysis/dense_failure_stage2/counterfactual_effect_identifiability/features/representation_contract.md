# Representation contract

For each branch, `r=[last valid text/control state; mean valid visual-token state]` immediately after the current layer. Raw compact valid-token tensors are stored in packed BF16 caches. PAIR order is FULL then OFF; DELTA is FULL minus OFF.
