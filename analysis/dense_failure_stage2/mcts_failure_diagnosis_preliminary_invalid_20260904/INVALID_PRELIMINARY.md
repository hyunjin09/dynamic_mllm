# Invalid preliminary aggregation

This directory is retained only as recoverable execution provenance. Its
summary selected a one-UID C5 prefix-forcing cohort as the "best" gain and
therefore must not be used for scientific interpretation.

The authoritative rerun is `../mcts_failure_diagnosis/`. It separates gains
caused by forced corrective actions from reproduction of still-unforced later
corrective actions and binds the corrected aggregation code into a new frozen
contract.
